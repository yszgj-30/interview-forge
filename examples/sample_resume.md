# 林晓 · AI 应用开发实习生（虚构示例）

> 本文件仅用于产品演示，人物、公司和指标均为虚构信息。

## 技能

Python、FastAPI、RAG、Ollama、SQLite、Docker、Pytest

## 项目经历

### 企业知识问答助手

- 我负责使用 FastAPI 构建文档问答接口，设计检索、重排、回答生成和引用返回链路。
- 为检索无结果场景增加相似度阈值和拒答策略，使用 120 条虚构测试问题进行离线评估。
- 加入 Redis 缓存与请求日志，在相同压测环境下将 P95 响应时间从 1.8 秒降低到 1.1 秒。
- 使用 Pytest 覆盖文档解析、检索回退和接口异常路径，并通过 Docker 提供本地运行环境。

### 面试训练助手

- 设计简历与岗位要求匹配、模拟问答、逐题评分和会后复盘工作流。
- 接入本地 Ollama，并在模型不可用时回退到确定性规则，避免核心流程中断。
